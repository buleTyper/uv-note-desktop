2026年8月5日18点09分

我的标签管理默认是前面的路径都满足且后面的标签都不满足才会出现在当前层及，和实际知识点之间的关系比较接近。

但也有一些例外，比方说：

#标签A / #标签B / 001(#标签B)
#标签C / #标签B

001不会出现在第二行的标签B下面，即使它有标签B，因为他不满足标签C。

有时候就是想让001出现在所有标签B下面不管前面有没有满足。有两种途径解决：  
1. 001文件打上特殊标记 `*#标签B`，*是通配符，表示这个文件会出现在所有B标签下
2. 给某个结构中打上标记，虽然在管理器显示这个标签，但其后文件不需要满足这个标签

