怎么合并两个文件呢，我一直很犯难

obsidain的做法就是直接删掉原文件，内容直接接到另一个文件的后面

那

1. 如果两个文件标题级别不一样呢，我经常一个文件标题从三级标题开始，因为一级标题太大了不美观，这个很难受，也许可以在合并之前强行提升到一级标题？还有我的一级标题一定要小一点
2. 还有文件名称，合并之后文件名称就不见了，我觉得可以保留一下


单纯的合并文件倒也不算很难，难的是怎么管理，能不能恢复到之前的状态，同时每个文件可以有分支


说起来，也许可以做一个假合并，用户可以选择假合并，用同一个文件存储，但用分割线分开，还是像两个单独文件渲染


。。。
合并文件的功能是应该要有的，有的时候很多个文件精炼到一定程度就都是一段话了，还占着单独的文件不好看。


那么要不要真合并，删掉原文件内容接到另一个文件。我觉得还是要的：  

还有另一个场景，精炼后的内容与另一个文件不是平级关系而是包含关系

例如我很久之前学的一个`cd`命令，在当时可能会用单独一个文件记录，但到了后面可能就是一个表格里面的短短一行。  
这种场景，正常笔记软件的做法一般是直接删掉这文件，但我为什么要做合并功能

因为分支功能，一个文件可以有多个分支，如果我删掉了其中一个分支，那其他分支怎么办

aaa
bbb


bbz