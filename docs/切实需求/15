举个例子：有次我复制了一个帮我生成ASCII树状图的优化过的详细提示词，但是我不经常用，我该记哪里？  

记到AI提示词文件夹？如果我忘记有这个提示词，只想看看有没有生成ASCII树状图的方法时，怎么办  

复制到 ASCII艺术 文件夹？会占用内存倒没什么，但是不方便，我也从来没有这样做过

所以我用这样一个方式：在文件里面输入 `#ASCII艺术`，让他自动在这个文件夹里面也显示，而且有补全建议，大概只要打 `AS` 就够了