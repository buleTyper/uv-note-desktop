我一开始想要实现程度设置功能的时候，想要用的实现方式就是在文件标签后加一个数字，根据数字进行排序。

但我发现很多类似功能都是不用数字，直接用有序数组实现，这样似乎是更好的选择，因为加上数字似乎就是纯拖累，没有作用。

可在实际体验中，很多时候我不会改变文件顺序，这时候如果我写了一篇很重要的内容，我想把它提到上面，可前面有很多旧文件，怎么办，估计要疯狂往上提。而加上数字可以很好的解决这个问题，前面的文件都没改过，值大概就是默认值，我只需要提一次，瞬间超过这些旧文件，显眼起来。
