# 图形化操作

初始界面：顶部菜单栏、左侧活动栏、底部状态栏和主工作区

## 主工作区

包含：主侧边栏、辅助侧边栏和编辑区
侧边栏有：文件列表、标签式管理器、日历


**Tab 标签栏**：打开文件进入*预览模式*，若无修改不固定，打开其他文件自动关闭；若有修改，进入固定模式，关闭位置显示小圆点，打开其他文件不关闭留在 Tab标签栏。


**装订线 Gutter**：包括行号边栏、折叠箭头和Git 状态线  
行号边栏默认三位数宽，超过后向右扩展保证第一位数字不动。
折叠箭头：多级标题、等可以折叠


**编辑区**：
符号淡化与语义高亮：Markdown 语法符号（如 #、**、*、` 等）使用暗色淡化，降低视觉干扰；标题文字保持统一字号，亮蓝加粗；加粗内容为纯白粗体；斜体、删除线、行内代码、链接均以不同颜色和样式呈现，突出内容本身。

图片粘贴处理：从剪贴板粘贴图片时自动保存到 .uvnote/assets/ 目录，并在源码中插入标准相对路径图片语法，无需手动管理附件。

内嵌图片预览：识别源码中的图片链接（网络地址、相对路径、隐藏目录），在对应行下方直接展示预览图。预览图不占用新行号，并可通过左侧折叠按钮收起或展开。

链接交互：按住 Ctrl 或 Cmd 并点击链接，可在新标签页中打开目标地址。

整体设计在保留完整源码的同时，提供了更舒适、聚焦内容的编辑和阅读体验

