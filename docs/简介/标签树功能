1）我可以设置多个标签结构，例如“#计算机#算法#动态规划”这样就是标签结构
2）一个标签后面可以有多个标签例如“#计算机#算法”和“#计算机#硬件”，但是这并不是父子关系，硬件标签可以在另一个标签串开头和计算机标签平齐
3）一个文件按照它所有的标签显示在标签式管理器，对于每一个分支，从第一个开始判断，如果没有子标签符合这个文件的所含有的标签，就在当前显示
4）当我在编辑的时候给文件打了一个标签，就去找所有包含这个标签的结构，往前面把所有父标签自动给他打上



---


我一次听说了筛选键的快捷键，想记录下来。放哪里呢，

放"Windows"文件夹吗，但是Windows"快捷键"文件夹也很适合，而且如果这个快捷键记那个快捷键不记，总结起来就会遗漏。

那不放windows文件夹里面，但是我记的就是筛选键的介绍啊，只放在快捷键里面，到时候没看到windows文件夹里面有就慢慢忘记了。

难道要两个文件夹都写一个文件。

而我设计的功能只要加一个 "#快捷键"，立刻解决这个问题